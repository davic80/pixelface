{
  "name": "pixelface",
  "version": "0.2.0",
  "description": "Pixela y censura caras en tus fotos. 100% privado: todo se procesa en tu navegador, la imagen nunca se sube.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "setup:assets": "node scripts/fetch-assets.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "biome check .",
    "format": "biome format --write .",
    "ci": "biome ci ."
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.18"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "vite": "^6.0.0"
  }
}
